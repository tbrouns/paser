Change line 8 in "preprocessData" from:

ops = convertOpenEphysToRawBInary(ops);  % convert data, only for OpenEphys

to:

ops = ept_kst_convert2binary(ops);  % convert data, only for OpenEphys

(we use a different function)